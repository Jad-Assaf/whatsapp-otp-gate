/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Next.js 14 automatically enables the app directory; no experimental flags needed.
};

module.exports = nextConfig;